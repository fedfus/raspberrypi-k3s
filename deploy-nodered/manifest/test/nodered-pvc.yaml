apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: node-red
  name: node-red-pvc
  namespace: nodered
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi